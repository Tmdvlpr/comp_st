# CS Monitor AI — environment variables
# Скопируй этот файл в `.env` и заполни реальными значениями.
# `.env` в .gitignore, в репозиторий не попадает.

# PostgreSQL (SCADA data source)
CS_DB_HOST=10.1.30.164
CS_DB_PORT=5432
CS_DB_NAME=CS
CS_DB_USER=postgres
CS_DB_PASSWORD=replace-me

# Uzex.uz integration API (fetch_deals.py)
UZEX_API_AUTH=Basic replace-me-base64-token
